## Overview

This section documents the configuration of the R1-GATEWAY router used in the Cisco Hybrid Enterprise Network Lab.

The router serves as the primary gateway for the on-premises network and provides connectivity between internal network devices and external network segments.

The configuration process included assigning IP addresses, enabling interfaces, and verifying connectivity between network devices.
